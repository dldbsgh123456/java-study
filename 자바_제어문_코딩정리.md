# 자바 기초 - 제어문(조건문/switch) 코드 정리

같은 로직이 여러 번 반복된 예제는 하나로 통합하고, 문법이 다른 버전(if vs switch, switch 클래식 vs 화살표)만 비교용으로 남겼다.

## 1. 단일 조건문 (if)

**가위바위보 승패 판정**
```java
import java.util.Scanner;

public class 제어문_단일조건문 {
	public static void main(String[] args) {
		int com = (int) (Math.random() * 3); // 0,1,2
		Scanner r = new Scanner(System.in);
		System.out.print("가위(0),바위(1),보(2):");
		int user = r.nextInt();

		int res = com - user;
		if (res == -1 || res == 2)
			System.out.println("사용자 win!!");
		if (res == 1 || res == -2)
			System.out.println("컴퓨터 win!!");
		if (res == 0)
			System.out.println("비겼다");
	}
}
```

**알파벳 대문자/소문자/숫자 판별**
```java
import java.util.Scanner;

public class 제어문_단일조건문_1 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("알파벳을 입력:");
		String ch = r.next();
		char c = ch.charAt(0); // 문자열에서 문자 하나 추출

		System.out.println("알파벳:" + c);
		if (c >= 'A' && c <= 'Z')
			System.out.println(c + "는(은) 대문자입니다.");
		if (c >= 'a' && c <= 'z')
			System.out.println(c + "는(은) 소문자입니다.");
		if (c >= '0' && c <= '9')
			System.out.println(c + "는(은) 숫자입니다.");
	}
}
```

**3의 배수 / 5의 배수 / 7의 배수 판별**
```java
public class 제어문_단일조건문_2 {
	public static void main(String[] args) {
		int num = 15;
		if (num % 3 == 0)
			System.out.println(num + "는(은) 3의배수입니다.");
		if (num % 5 == 0)
			System.out.println(num + "는(은) 5의배수입니다.");
		if (num % 7 == 0)
			System.out.println(num + "는(은) 7의배수입니다.");
	}
}
```

**짝수/홀수 판별**
```java
import java.util.Scanner;

public class 제어문_종류 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("정수 입력:");
		int num = r.nextInt();

		if (num % 2 == 0)
			System.out.println(num + "는(은) 짝수입니다.");
		if (num % 2 != 0)
			System.out.println(num + "는(은) 홀수입니다.");
	}
}
```

---

## 2. 선택 조건문 (if~else)

**최댓값/최솟값 비교** (삼항연산자 + if로 이름표만 확인하는 예제)
```java
import java.util.Scanner;

public class 제어문_선택조건문 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.println("첫번째 정수 입력:");
		int num1 = r.nextInt();
		System.out.println("두번째 정수 입력:");
		int num2 = r.nextInt();

		int num3 = num1 > num2 ? num1 : num2;
		if (num3 == num1)
			System.out.println("최대값:" + num3);
		if (num3 == num2)
			System.out.println("최대값:" + num3);
		// 참고: 최솟값을 함께 보려면 num1 < num2 ? num1 : num2 로 별도 계산 필요
	}
}
```

**로그인 처리**
```java
import java.util.Scanner;

public class 제어문_선택조건문_1 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("ID 입력:");
		String id = r.next();
		System.out.print("비밀번호 입력:");
		String pwd = r.next();

		// 주의: 문자열은 == 대신 .equals()로 비교해야 정확함
		if (id.equals("admin") && pwd.equals("1234")) {
			System.out.println("로그인 되었습니다");
		} else {
			System.out.println("ID나 Password가 틀렸습니다.");
		}
	}
}
```

**합격/불합격 판정 (평균 60점 기준)**
```java
import java.util.Scanner;

public class 제어문_선택조건문_2 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("국어점수 입력:");
		int kor = r.nextInt();
		System.out.print("영어점수 입력:");
		int eng = r.nextInt();
		System.out.print("수학점수 입력:");
		int math = r.nextInt();

		int avg = (kor + eng + math) / 3;
		if (avg >= 60) {
			System.out.println("합격");
		} else {
			System.out.println("불합격");
		}
	}
}
```

**절대값 출력**
```java
import java.util.Scanner;

public class 제어문_선택조건문_3 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("정수 입력:");
		int num = r.nextInt();

		System.out.println("검증:" + Math.abs(num)); // 라이브러리 사용

		if (num < 0) {
			System.out.println(num * -1);
		} else {
			System.out.println(num);
		}
	}
}
```

---

## 3. 다중 조건문 (if~else if~else)

**사칙연산 계산기**
```java
import java.util.Scanner;

public class 제어문_다중조건문 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("첫번째 정수입력:");
		int num = r.nextInt();
		System.out.print("연산자 입력(+,-,*,/):");
		String op = r.next();
		System.out.print("두번째 정수입력:");
		int num1 = r.nextInt();

		if (op.equals("+")) {
			System.out.printf("%d+%d=%d\n", num, num1, num + num1);
		} else if (op.equals("-")) {
			System.out.printf("%d-%d=%d\n", num, num1, num - num1);
		} else if (op.equals("*")) {
			System.out.printf("%d*%d=%d\n", num, num1, num * num1);
		} else if (op.equals("/")) {
			if (num1 == 0)
				System.out.println("0으로 나눌 수 없습니다");
			else
				System.out.printf("%d/%d=%d\n", num, num1, num / num1);
		} else {
			System.out.println("연산자입력이 잘못되었습니다");
		}
	}
}
```

**월 입력 → 계절 판별 (범위 검증 포함)**
```java
import java.util.Scanner;

public class 제어문_조건문 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("월 입력:");
		int no = r.nextInt();

		if (no >= 1 && no <= 12) {
			if (no >= 3 && no <= 5)
				System.out.println("계절은 봄 입니다.");
			else if (no >= 6 && no <= 8)
				System.out.println("계절은 여름 입니다.");
			else if (no >= 9 && no <= 11)
				System.out.println("계절은 가을 입니다.");
			else
				System.out.println("계절은 겨울 입니다.");
		} else {
			System.out.println("없는 달입니다.");
		}
	}
}
```

**국/영/수 총점·평균·학점 (A~F)**
```java
import java.util.Scanner;

public class 제어문_다중조건문_2 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("국어 입력:");
		int kor = r.nextInt();
		System.out.print("영어 입력:");
		int eng = r.nextInt();
		System.out.print("수학 입력:");
		int math = r.nextInt();

		int total = kor + eng + math;
		double avg = total / 3.0;

		char score;
		if (avg >= 90) score = 'A';
		else if (avg >= 80) score = 'B';
		else if (avg >= 70) score = 'C';
		else if (avg >= 60) score = 'D';
		else score = 'F';

		System.out.println("총점:" + total);
		System.out.println("평균:" + avg);
		System.out.println("학점:" + score);
	}
}
```

**세부 등급까지 판별 (A+/A0/A- ...), 난수 점수**
```java
public class 제어문_다중조건문_3 {
	public static void main(String[] args) {
		int kor = (int) (Math.random() * 50) + 51; // 51~100
		int eng = (int) (Math.random() * 50) + 51;
		int math = (int) (Math.random() * 50) + 51;

		System.out.println("국어점수:" + kor);
		System.out.println("영어점수:" + eng);
		System.out.println("수학점수:" + math);

		int total = kor + eng + math;
		int avg = total / 3;

		char score = ' ';
		char op = ' ';

		if (avg >= 90) {
			score = 'A';
			if (avg >= 97) op = '+';
			else if (avg >= 94) op = '0';
			else op = '-';
		} else if (avg >= 80) {
			score = 'B';
			if (avg >= 87) op = '+';
			else if (avg >= 84) op = '0';
			else op = '-';
		} else if (avg >= 70) {
			score = 'C';
			if (avg >= 77) op = '+';
			else if (avg >= 74) op = '0';
			else op = '-';
		} else if (avg >= 60) {
			score = 'D';
			if (avg >= 67) op = '+';
			else if (avg >= 64) op = '0';
			else op = '-';
		} else {
			score = 'F';
			op = ' ';
		}
		System.out.println("======== 결과 =========");
		System.out.println("총점:" + total);
		System.out.println("평균:" + avg);
		System.out.println("학점:" + score + op);
	}
}
```

**메뉴 선택 → 외부 사이트에서 차트 크롤링 (다중조건문 활용 예)**
```java
import java.util.Scanner;
import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;
import org.jsoup.select.Elements;

public class 제어문_다중조건문_1 {
	public static void main(String[] args) throws Exception {
		Scanner scan = new Scanner(System.in);
		System.out.print("메뉴 선택(가요(1) POP(2) OST(3) 트롯(4) JAZZ(5) CLASSIC(6)):");
		int menu = scan.nextInt();

		String baseurl = "https://www.genie.co.kr/chart/genre?ditc=D&ymd=20260305&genrecode=";
		String url = "";
		if (menu == 1) url = "M0100";
		else if (menu == 2) url = "M0200";
		else if (menu == 3) url = "M0300";
		else if (menu == 4) url = "M0107";
		else if (menu == 5) url = "M0500";
		else if (menu == 6) url = "M0600";
		else System.out.println("없는 메뉴입니다!!");

		Document doc = Jsoup.connect(baseurl + url).get();
		Elements title = doc.select("table.list-wrap a.tile");
		for (int i = 0; i < title.size(); i++) {
			System.out.println((i + 1) + "." + title.get(i).text());
		}
	}
}
```

---

## 4. 선택문 (switch)

**클래식 switch — 학점 처리 (case fall-through 활용)**
```java
public class 제어문_선택문 {
	public static void main(String[] args) {
		int no = 99;
		char op = 'A';

		switch (no / 10) {
		case 10:
		case 9:
			op = 'A';
			break;
		case 8:
			op = 'B';
			break;
		case 7:
			op = 'C';
			break;
		case 6:
			op = 'D';
			break;
		default:
			op = 'F';
		}
		System.out.println(op);
	}
}
```

**switch 표현식(화살표, JDK14+) — 학점 처리**
```java
import java.util.Scanner;

public class 제어문_선택문_1 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("점수 입력:");
		int score = r.nextInt();

		String grade = switch (score / 10) {
		case 10, 9 -> "A";
		case 8 -> "B";
		case 7 -> "C";
		case 6 -> "D";
		default -> "F";
		};
		System.out.println("학점:" + grade);
	}
}
```

**switch 표현식 — 월 → 계절 판별**
```java
import java.util.Scanner;

public class 제어문_계절_스위치 {
	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		System.out.print("월 입력:");
		int mon = scan.nextInt();

		switch (mon) {
		case 3, 4, 5 -> System.out.println("봄 입니다");
		case 6, 7, 8 -> System.out.println("여름 입니다");
		case 9, 10, 11 -> System.out.println("가을 입니다");
		case 12, 1, 2 -> System.out.println("겨울 입니다");
		default -> System.out.println("없는 달입니다");
		}
	}
}
```

**반복 메뉴 처리 (while + switch + 종료)**
```java
import java.util.Scanner;

public class 제어문_선택문_2 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		while (true) {
			System.out.println("1.가요");
			System.out.println("2.트롯");
			System.out.println("3.ost");
			System.out.println("4.JAZZ");
			System.out.println("5.POP");
			System.out.println("6.CLASSIC");
			System.out.println("7.종료");
			System.out.println("=================");
			System.out.print("메뉴 선택:");
			int menu = r.nextInt();

			switch (menu) {
			case 7:
				System.out.println("프로그램 종료");
				System.exit(0);
			}
		}
	}
}
```

**가위바위보 (switch 버전)**
```java
import java.util.Scanner;

public class 제어문_선택문_3 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		int com = (int) (Math.random() * 3);
		System.out.print("가위(0),바위(1),보(2):");
		int user = r.nextInt();

		if (user >= 0 && user <= 2) {
			System.out.print("컴퓨터:");
			switch (com) {
			case 0: System.out.print("가위"); break;
			case 1: System.out.print("바위"); break;
			case 2: System.out.print("보");
			}
			System.out.println();

			System.out.print("사용자:");
			switch (user) {
			case 0: System.out.println("가위"); break;
			case 1: System.out.println("바위"); break;
			case 2: System.out.println("보");
			}

			System.out.println("\n ========= 결과 =========");
			switch (com - user) {
			case 1: case -2: System.out.println("컴퓨터 win"); break;
			case -1: case 2: System.out.println("사용자 win"); break;
			case 0: System.out.println("비겼다");
			}
		} else {
			System.out.println("Game Over");
		}
	}
}
```

**사칙연산 계산기 — switch 클래식 vs 화살표 문법 비교**
```java
import java.util.Scanner;

public class 제어문_선택문_4 {
	public static void main(String[] args) {
		Scanner r = new Scanner(System.in);
		System.out.print("첫번째 정수:");
		int num1 = r.nextInt();
		System.out.print("연산자 입력(+,-,*,/):");
		String op = r.next();
		System.out.print("두번째 정수:");
		int num2 = r.nextInt();

		// 클래식 문법
		switch (op) {
		case "+":
			System.out.println(num1 + "+" + num2 + "=" + (num1 + num2));
			break;
		case "-":
			System.out.printf("%d-%d=%d\n", num1, num2, num1 - num2);
			break;
		case "*":
			System.out.printf("%d*%d=%d\n", num1, num2, num1 * num2);
			break;
		case "/":
			if (num2 == 0)
				System.out.println("0으로 나눌 수 없습니다");
			else
				System.out.printf("%d/%d=%d\n", num1, num2, num1 / num2);
			break;
		default:
			System.out.println("연산처리를 할 수 없습니다.");
		}

		// 화살표 문법 (여러 문장이 필요하면 {} 블록 사용)
		switch (op) {
		case "+" -> {
			System.out.printf("%d+%d=%d\n", num1, num2, num1 + num2);
			System.out.println("프로그램 종료");
		}
		case "-" -> System.out.printf("%d-%d=%d\n", num1, num2, num1 - num2);
		case "*" -> System.out.printf("%d*%d=%d\n", num1, num2, num1 * num2);
		case "/" -> System.out.printf("%d/%d=%d\n", num1, num2, num1 / num2);
		default -> System.out.println("없는 연산자입니다.");
		}
	}
}
```
